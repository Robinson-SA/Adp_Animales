==Etapa 2==
crea una aplicacion web, simple, ordenda que permita ver mascotas, destinigir su especie, resaltar los casos de adpocion urgente, mascotacard y los datos esta en mascostas.jsx

como buenas practicas define lso tipos propTypes, agrega un estilo diferente para perros y gatos, ajusta bien div donde esta el nombre de la especie paraque todos tenga el mismo tamaño

repuesta: sirvio correctamente si tiene que ajustar los tamaños de algunos "div donde esta las especie", se hizo otro promps para vereficar las buenas practicas y agregar un estilos diferente a cada especie.

== Etapa 3==
modifica el listado de mascota usa props, Asigna prop key usando el id de cada elemento en App.jsx renderiza los datos importados

repuesta:util cumple con lo pedido se utilizo correctamte los props para asegura

==Etapa 4==
aplica un filtro de mascotas utilizando props .map(), por especie,estado,años, por estiquetas, asigna props key usando el id de cada elemento,aplica un busqueda por nombre , tienes que noramalizar textoy aplicar buenas practicas y si no cunetra coloca un mensaje "No hay mascotas que coincidan"

repuesta:el filtro funciono de manenara correcta filtara por estiqueta, estado y especie sirve de manera correcta y los datos esta bien y son corente 


== Etapa 5==
quiero que cambies la forma en la que se ve las mascostas urgentes(color, los bordes de forma en la que se haga notar) se el contador de de posicion y como se ve

repuesta: si cambio el como se ve los urgentes pero agrego unos numeros los cual quitare